version: 0.2
phases:
  pre_build:
    commands:
      - echo "1. pre build step"
      - aws ecr get-login-password --region ap-southeast-1 | docker login --username AWS --password-stdin 666402361323.dkr.ecr.ap-southeast-1.amazonaws.com
  build:
    commands:
      - echo "2. Build step (docker image)"
      - docker build -t nginx:v1.0.0
  post_build:
    commands:
      - echo 3. "Post build"
      - docker tag nginx:v1.0.0 666402361323.dkr.ecr.ap-southeast-1.amazonaws.com/do02:latest
      - docker push 666402361323.dkr.ecr.ap-southeast-1.amazonaws.com/do02:latest

artifacts:
  files:
    - codedeploy/**/*
    - appspec.yml
    - docker-compose.yml
    - .env
